# Optional: dev-only fallback for the Anthropic API key.
# In production the user pastes their own key into Settings; it is stored
# in IndexedDB and used to call api.anthropic.com directly from the browser.
# NEXT_PUBLIC_ANTHROPIC_API_KEY=sk-ant-...
